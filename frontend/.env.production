# Intentionally unset: api.js falls back to the relative "/api" path, which
# vercel.json rewrites to the Python function. Same-origin, no domain to
# maintain here, and it works for both production and preview deployments.
